import { ListStatus } from "../types/types";

const listsStatus: ListStatus[] = [
  { id: "open", title: "To Do" },
  { id: "inProgress", title: "In Progress" },
  { id: "done", title: "Done" },
];

export default listsStatus;
